import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.transport.v1',
  appName: 'TransportApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
